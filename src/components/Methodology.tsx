import { useEffect, useRef } from 'react';

export function Methodology({ close }: { close: () => void }) {
  const dialog = useRef<HTMLDialogElement>(null);
  useEffect(() => { const d = dialog.current!; d.showModal(); return () => d.close(); }, []);
  return <dialog ref={dialog} className="methodology" aria-labelledby="methodology-title" onCancel={close} onClick={e => { if (e.target === e.currentTarget) close(); }}><div className="panel-heading"><h2 id="methodology-title">Reading the atlas</h2><button className="icon-button" onClick={close} aria-label="Close methodology">×</button></div><p>This is a curated map of programming ideas, not an exhaustive family tree.</p><h3>What the marks mean</h3><p>A filled dot marks a documented introduction; smaller dots mark selected changes. A track provides historical context and does not imply active maintenance. Clusters expand when selected.</p><h3>Dates have context</h3><p>Each introduction names the release or publication being used. “c.” marks approximate dates; intervals represent uncertainty. A language may have existed privately before its first publication.</p><h3>An arrow is a claim</h3><p>Connections describe documented influence, with citations and an explanation of what changed. A source-language anchor does not establish the exact date of an idea. Disputed claims use dashed lines and explicit caveats. Similar syntax alone is not evidence.</p><h3>Selected years, broader history</h3><p>Filtering shows changes during the period, including changes to older languages. Related sources remain available as context. The comparison panel also includes influences outside the selected window, labeled accordingly.</p><h3>Open to correction</h3><p>Coverage is incomplete. We use primary sources where available, with Wikipedia as an additional historical reference. Original explanations and curated data are CC BY 4.0; code is MIT. Linked sources retain their own licenses.</p></dialog>;
}
